import SwiftUI

@main
struct ToodlesApp: App {
    init() {
        // FirebaseApp.configure() will be enabled in Phase 1 Task 1.3
        // after GoogleService-Info.plist is present in the bundle.
    }

    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
